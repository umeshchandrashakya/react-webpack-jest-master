import * as React from "react";
import LoginFormModule from "../login/loginmodule";

export const LoginComponent = () => {
  return (
    <div>
      <LoginFormModule></LoginFormModule>
    </div>
  );
};
